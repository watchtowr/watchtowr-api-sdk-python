"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from enum import Enum
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict
from watchtowr_complete_watchtowr_platform_api_documentation_python.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)


class RetestRetestStatus(str, Enum):
    STARTED = "started"
    IN_PROGRESS = "in-progress"
    SUCCESS = "success"
    ERROR = "error"


class CurrentRetestTypedDict(TypedDict):
    requested_by: str
    requested_at: datetime
    retest_status: RetestRetestStatus
    status_occurred_at: NotRequired[Nullable[datetime]]
    completed_at: NotRequired[Nullable[datetime]]
    evidence: NotRequired[Nullable[str]]


class CurrentRetest(BaseModel):
    requested_by: str

    requested_at: datetime

    retest_status: RetestRetestStatus

    status_occurred_at: OptionalNullable[datetime] = UNSET

    completed_at: OptionalNullable[datetime] = UNSET

    evidence: OptionalNullable[str] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = ["status_occurred_at", "completed_at", "evidence"]
        nullable_fields = ["status_occurred_at", "completed_at", "evidence"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m


class RetestTypedDict(TypedDict):
    retest_remaining: NotRequired[float]
    r"""The number of remaining test cases"""
    current_retest: NotRequired[Nullable[CurrentRetestTypedDict]]


class Retest(BaseModel):
    retest_remaining: Optional[float] = None
    r"""The number of remaining test cases"""

    current_retest: OptionalNullable[CurrentRetest] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = ["retest_remaining", "current_retest"]
        nullable_fields = ["current_retest"]
        null_default_fields = []

        serialized = handler(self)

        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k)
            serialized.pop(k, None)

            optional_nullable = k in optional_fields and k in nullable_fields
            is_set = (
                self.__pydantic_fields_set__.intersection({n})
                or k in null_default_fields
            )  # pylint: disable=no-member

            if val is not None and val != UNSET_SENTINEL:
                m[k] = val
            elif val != UNSET_SENTINEL and (
                not k in optional_fields or (optional_nullable and is_set)
            ):
                m[k] = val

        return m
